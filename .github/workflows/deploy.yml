name: Deploy to AWS

on:
  push:
    branches:
      - master
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Deploy to S3
        run: |
          # Deploy root landing assets
          aws s3 cp index.html s3://${{ secrets.S3_BUCKET }}/index.html \
            --cache-control "public, max-age=3600" \
            --content-type "text/html"

          # Deploy day 1
          aws s3 sync day1/ s3://${{ secrets.S3_BUCKET }}/day1/ \
            --delete \
            --cache-control "public, max-age=3600"

          # Deploy day 2
          if [ -d day2 ]; then
            aws s3 sync day2/ s3://${{ secrets.S3_BUCKET }}/day2/ \
              --delete \
              --cache-control "public, max-age=3600"
          fi

          # Deploy day 3
          if [ -d day3 ]; then
            aws s3 sync day3/ s3://${{ secrets.S3_BUCKET }}/day3/ \
              --delete \
              --cache-control "public, max-age=3600"
          fi

          # Deploy day 4
          if [ -d day4 ]; then
            aws s3 sync day4/ s3://${{ secrets.S3_BUCKET }}/day4/ \
              --delete \
              --cache-control "public, max-age=3600"
          fi

          # Deploy resources page
          if [ -d resources ]; then
            aws s3 sync resources/ s3://${{ secrets.S3_BUCKET }}/resources/ \
              --delete \
              --cache-control "public, max-age=3600"
          fi

          # Deploy practice exams page
          if [ -d practice-exams ]; then
            aws s3 sync practice-exams/ s3://${{ secrets.S3_BUCKET }}/practice-exams/ \
              --delete \
              --cache-control "public, max-age=3600"
          fi

          # Deploy shared static assets
          if [ -d assets ]; then
            aws s3 sync assets/ s3://${{ secrets.S3_BUCKET }}/assets/ \
              --delete \
              --cache-control "public, max-age=3600"
          fi

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.CLOUDFRONT_DIST_ID }} \
            --paths "/*"

      - name: Deployment complete
        run: |
          echo "✅ Deployment complete!"
          echo "Site: https://aif.atla.in/day1/"
